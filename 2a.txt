#Study how to create a process in UNIX using fork() system call.

#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
void bubbleSort(int arr[],int n)
{
for(int i=0;i<n-1;i++)
{
for(int j=0;j<n-i-1;j++)
{
if(arr[j]>arr[j+1])
{
int temp=arr[j];
arr[j]=arr[j+1];
arr[j+1]=temp;
}
}
}
}
void selectionSort(int arr[],int n)
{
for(int i=0;i<n-1;i++)
{
int min=i;
for(int j=i+1;j<n;j++)
{
if(arr[j]<arr[min])
{
min=j;
}
}
int temp=arr[i];
arr[i]=arr[min];
arr[min]=temp;
}
}
int main()
{
int n;
printf("\n Enter the number of elements:");
scanf("%d",&n);int arr[n];
printf("\n Enter %d integers:\n",n);
for(int i=0;i<n;i++)
{
scanf("%d",&arr[i]);
}
pid_t pid=fork();
if(pid<0)
{
printf("\n fork failed...!");
return 1;
}
else if(pid ==0)
{
printf("\n Child process(PID:%d) sording using Bubble sort...\n",getpid());
bubbleSort(arr,n);
printf("\n child process sorted array (Bubble sort):");
for(int i=0;i<n;i++)
printf("\t %d",arr[i]);
printf("\n");
exit(0);
}
else
{
sleep(5);
printf("\n Parent process(PID:%d)sording using selection sort .... \n",getpid());
selectionSort(arr,n);
printf("\n parent process sorted array (selection sort):");
for(int i=0;i<n;i++)
printf("\t %d",arr[i]);
printf("\n");
}
return 0;
}

#OUTPUT
svpmit31@svpmit31-HP-EliteDesk-800-G2-SFF:~/Documents$ gcc 2a.c -o 2a
svpmit31@svpmit31-HP-EliteDesk-800-G2-SFF:~/Documents$ ./2a

 Enter the number of elements:5

 Enter 5 integers:
9
8
7
6
5

 Child process(PID:3862) sording using Bubble sort...

 child process sorted array (Bubble sort):	 5	 6	 7	 8	 9

 Parent process(PID:3856)sording using selection sort .... 

 parent process sorted array (selection sort):	 5	 6	 7	 8	 9

